#include<iostream>
#include<string>
#include<bits/stdc++.h>
#include<math.h>

using namespace std;

int main(){
    int a, b, c;
    cin >> a;
    cin >> b;
    cin >> c;
    int array 
    for (i = 0, i < max({a,b,c}), i++) {
        int funcao = abs(a - a) + pow(abs(a - b), 2) + pow(abs(a - c), 3);

    }
    

    cout << min({funcaoA,funcaoB,funcaoC});
}